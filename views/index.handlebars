<div class="container">
    <div class="row">
        <img src=>
        <h1>Burgerlogger</h1>
    </div>
</div>
<div class="row">
    <div class="col-md-6 text-center" class="task">
        {{#each burger_data}}
        {{#unless this.eaten}}
        <div class="col-md-9 text-center">
        </div>